{
  "name": "cloud-satchel",
  "private": true,
  "version": "0.7.1",
  "type": "module",
  "scripts": {
    "build": "npm --prefix ui run build",
    "tauri": "tauri",
    "release": "npm run tauri build && powershell -NoProfile -ExecutionPolicy Bypass -File dev/_rename_installer.ps1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
